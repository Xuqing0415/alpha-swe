# 技能：Jest 测试约定（上下文建议）
name: jest
version: "1.0.0"
description: Jest 测试规范（describe/it、mock、coverage）
priority: 4
requires: []
permissions: [file_read, file_write, terminal_execute]
tags: [testing, jest]
triggers:
  project_dep: [jest]
steps:
  - name: apply-conventions
    instruction: 按 Jest 约定编写/调整测试（describe/it/mock）并运行验证
    dependencies: []
