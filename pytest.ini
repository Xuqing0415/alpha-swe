[pytest]
pythonpath = tests
norecursedirs = test_workspace .git .venv node_modules __pycache__ .idea .pytest_cache .mypy_cache benchmarks
markers =
    chaos: CI 混沌阶段：基准集 + 随机故障注入的降级/优雅率验证
    docker: CI/本地真实 Docker daemon 可用时运行的沙箱验证（不可达自动跳过）
